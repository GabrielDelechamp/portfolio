import { Fira_Code } from 'next/font/google';
import { Lusitana } from 'next/font/google';
 
export const inter = Fira_Code({ subsets: ['latin'] });


export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});